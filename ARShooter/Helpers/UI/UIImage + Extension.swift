import UIKit

extension UIImage {
    static let background = UIImage(named: "background.jpg") ?? UIImage()
    static let menuBackground = UIImage(named: "menuBackground.jpg") ?? UIImage()
    static let sphereTexture = UIImage(named: "sphereTexture") ?? UIImage()
    static let crosshair = UIImage(named: "crosshair") ?? UIImage()
    static let shoot = UIImage(named: "shootImage") ?? UIImage()
    static let xMark = UIImage(systemName: "xmark.circle") ?? UIImage()
    
    //Backround node images
    static let plusX = UIImage(named: "plusX.png") ?? UIImage()
    static let minusX = UIImage(named: "minusX.png") ?? UIImage()
    static let plusY = UIImage(named: "plusY.png") ?? UIImage()
    static let minusY = UIImage(named: "minusY.png") ?? UIImage()
    static let plusZ = UIImage(named: "plusZ.png") ?? UIImage()
    static let minusZ = UIImage(named: "minusZ.png") ?? UIImage()
}
